%h1 Listing users

%table
  %thead
    %tr
      %th Name
      %th Email

  %tbody
    - @users.each do |user|
      %tr
        %td= user.name
        %td= user.email

%br

= link_to 'New User', new_user_path
